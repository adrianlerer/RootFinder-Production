{
  "name": "rootfinder-legal-ai",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "wrangler pages dev dist",
    "deploy": "npm run build && wrangler pages deploy dist --project-name rootfinder-legal-ai"
  },
  "dependencies": {
    "hono": "^4.9.5"
  },
  "devDependencies": {
    "@hono/vite-build": "^1.2.0",
    "@hono/vite-dev-server": "^0.18.2",
    "@hono/vite-cloudflare-pages": "^0.4.2",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "vite": "^6.3.5",
    "wrangler": "^4.4.0"
  }
}